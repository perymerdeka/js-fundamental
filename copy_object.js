const MyId = {
    'name': 'feri lukmansyah',
    'age': 20,
    'testimony': 'ini adalah testimony saya',
}

// copy array
const my_array = [1, 2];
const array_baru = [...my_array];

console.log(array_baru)

// copy object
const NewObjects = {...MyId};
console.log(NewObjects);